{
  "name": "clipshare",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "turbo dev --filter=@clipshare/*",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.3.0"
  },
  "packageManager": "pnpm@8.15.0",
  "engines": {
    "node": ">=18"
  }
}
